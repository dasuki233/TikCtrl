<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Instant App 支持 -->
    <dist:module dist:instant="true" />

    <!-- 必需的硬件特性 -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- 必需的权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
<!--    <uses-permission android:name="android.permission.RECORD_AUDIO" />-->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />  <!-- 新增的 -->
<!--    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />   &lt;!&ndash; 新增的 &ndash;&gt;-->


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <!-- 主界面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        <!--android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"> 新增的 -->

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <meta-data
            android:name="android.notch_support"
            android:value="true" />
    </activity>

    <!-- 手势识别后台服务 -->
        <service
            android:name=".HandGestureService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- 执行动作的无障碍服务 -->
        <service
            android:name=".GestureActionService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/gesture_action_service_config" />
        </service>

        <!-- 手势映射配置界面 -->
        <activity
            android:name=".GestureMappingActivity"
            android:exported="true" />


    </application>
</manifest>
